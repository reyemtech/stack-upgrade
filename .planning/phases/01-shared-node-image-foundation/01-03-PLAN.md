---
phase: 01-shared-node-image-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - stacks/node/scripts/ralph-loop.sh
  - stacks/node/scripts/stream-pretty.sh
  - stacks/node/kickoff-prompt.txt
autonomous: true
requirements:
  - INFRA-06
  - INFRA-09

must_haves:
  truths:
    - "Ralph loop restarts Claude Code on exit if checklist has incomplete tasks"
    - "Ralph loop writes status.json after each restart with phase, status, restarts, and elapsed time"
    - "Ralph loop stops after MAX_RESTARTS attempts or when all tasks are complete"
    - "After-run dependency snapshots (after-package-deps.json, after-lockfile-hash.txt) appear in /output/"
    - "result.json is written with outcome, exit_code, total_phases, completed, failed, restarts, elapsed, branch"
  artifacts:
    - path: "stacks/node/scripts/ralph-loop.sh"
      provides: "Restart loop for Claude Code sessions"
      contains: "MAX_RESTARTS"
    - path: "stacks/node/scripts/stream-pretty.sh"
      provides: "Stream JSON prettifier (stack-agnostic)"
      contains: "stream"
    - path: "stacks/node/kickoff-prompt.txt"
      provides: "Initial prompt for Claude Code"
      contains: ".upgrade"
  key_links:
    - from: "stacks/node/scripts/ralph-loop.sh"
      to: "stacks/node/kickoff-prompt.txt"
      via: "cat /skill/kickoff-prompt.txt passed to claude -p"
      pattern: "kickoff-prompt"
    - from: "stacks/node/scripts/ralph-loop.sh"
      to: "/output/result.json"
      via: "jq -n writes structured result"
      pattern: "result\\.json"
    - from: "stacks/node/scripts/ralph-loop.sh"
      to: "/output/status.json"
      via: "write_status function"
      pattern: "status\\.json"
---

<objective>
Create the ralph loop (restart harness), stream prettifier, and kickoff prompt for the shared Node image.

Purpose: The ralph loop is the critical resilience mechanism that restarts Claude Code if it exits before the upgrade checklist is complete (due to context exhaustion, errors, etc.). It also handles post-run artifact collection, dependency snapshots, push, and PR creation. The kickoff prompt is the initial instruction sent to Claude Code on each launch.

Output: Three files in `stacks/node/` that complete the run lifecycle: ralph-loop.sh (restart + post-run), stream-pretty.sh (log formatting), kickoff-prompt.txt (agent prompt).
</objective>

<execution_context>
@/Users/mariomeyer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mariomeyer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-node-image-foundation/01-CONTEXT.md
@.planning/phases/01-shared-node-image-foundation/01-RESEARCH.md
@.planning/phases/01-shared-node-image-foundation/01-01-SUMMARY.md

<interfaces>
<!-- ralph-loop.sh from Laravel is the direct reference. Port with JS-specific snapshot commands. -->

From stacks/laravel/scripts/ralph-loop.sh (reference — full file):
- write_status() function: writes JSON to /output/status.json with jq -n
- Main while loop: RESTARTS < MAX_RESTARTS
- Claude Code invocation: claude --dangerously-skip-permissions --verbose --max-turns $MAX_TURNS --output-format stream-json -p "$(cat /skill/kickoff-prompt.txt)"
- Pipe through stream-pretty.sh, tee to /output/claude-run-N.jsonl
- Checklist completion check: grep for "status: not_started|status: in_progress" in checklist.yaml
- Post-run: after-snapshots, copy artifacts to /output/, build result.json
- Push + PR creation if GIT_PUSH=true and GH_TOKEN set

Key changes for JS adaptation:
- Remove: composer show --format=json, php artisan --version, npm ls --json (all PHP-specific)
- Add: jq on package.json for after-package-deps.json, lockfile hash for after-lockfile-hash.txt
- Update: branch template uses STACK_TYPE and TARGET_VERSION
- Keep: all restart/status logic, result.json construction, push/PR logic

From stacks/laravel/scripts/stream-pretty.sh:
- Stack-agnostic — copy verbatim
- Prettifies Claude Code's stream-json output for readable logs

From stacks/node/entrypoint.sh (created by Plan 01):
- Exports: BRANCH, STACK_TYPE, PKG_MANAGER, TARGET_VERSION
- These env vars available to ralph-loop.sh
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ralph-loop.sh adapted for JS</name>
  <files>stacks/node/scripts/ralph-loop.sh</files>
  <action>
Port `stacks/laravel/scripts/ralph-loop.sh` with JS-specific adaptations. Use the Laravel file as the direct source — copy structure and logic, only change the snapshot commands and branch references.

**Identical to Laravel** (copy verbatim):
- Shebang, MAX_RESTARTS/MAX_TURNS defaults, RESTARTS counter, START_TIME, EXIT_CODE
- `write_status()` function (jq -n with phase, status, restarts, elapsed_minutes)
- Initial `write_status "0" "starting"`
- Main while loop structure
- CURRENT_PHASE extraction from checklist.yaml via grep
- Claude Code invocation: `claude --dangerously-skip-permissions --verbose --max-turns "$MAX_TURNS" --output-format stream-json -p "$(cat /skill/kickoff-prompt.txt)" 2>&1 | tee /output/claude-run-$((RESTARTS + 1)).jsonl | /skill/scripts/stream-pretty.sh || true`
- Checklist completion check (grep for not_started/in_progress)
- Restart counter increment and logging to run-log.md
- Max restarts exit with halted status
- Post-run: copy artifacts (run-log.md, checklist.yaml, plan.md, changelog.md) to /output/
- Post-run: git log --oneline to /output/commits.log
- result.json construction with jq -n
- Push + PR creation logic

**Changed from Laravel** (JS-specific):
1. **After-snapshots** (replace lines 64-68 of Laravel ralph-loop.sh):
   Replace `composer show`, `npm ls --json`, `php artisan --version` with:
   ```bash
   echo "Capturing post-upgrade dependency snapshots..."
   cd /workspace
   jq '{dependencies: (.dependencies // {}), devDependencies: (.devDependencies // {})}' \
     package.json > /output/after-package-deps.json 2>/dev/null || echo "{}" > /output/after-package-deps.json
   for lockfile in package-lock.json yarn.lock pnpm-lock.yaml; do
     if [ -f "$lockfile" ]; then
       sha256sum "$lockfile" > /output/after-lockfile-hash.txt
       break
     fi
   done
   ```

2. **Branch reference** in result.json:
   Replace `${BRANCH:-upgrade/laravel-${TARGET_LARAVEL}}` with `${BRANCH:-upgrade/node-${STACK_TYPE:-unknown}}"`

3. **PR title**:
   Replace `"Upgrade to Laravel ${TARGET_LARAVEL}"` with a dynamic title based on STACK_TYPE:
   ```bash
   case "$STACK_TYPE" in
     nextjs) PR_TITLE="Upgrade Next.js to ${TARGET_NEXTJS:-latest}" ;;
     cra) PR_TITLE="Migrate CRA to Vite" ;;
     vite-react) PR_TITLE="Upgrade Vite + React" ;;
     *) PR_TITLE="JS Stack Upgrade" ;;
   esac
   ```

Everything else transfers directly. Do NOT rewrite the push/PR logic — keep the same git push, GH_TOKEN check, gh pr create, DEFAULT_BRANCH detection pattern from Laravel.

Ensure `#!/bin/bash` shebang, no `set -e` (ralph loop handles errors manually).
  </action>
  <verify>
    <automated>cd /Users/mariomeyer/code/ReyemTech/stack-upgrade && test -f stacks/node/scripts/ralph-loop.sh && grep -q "MAX_RESTARTS" stacks/node/scripts/ralph-loop.sh && grep -q "write_status" stacks/node/scripts/ralph-loop.sh && grep -q "after-package-deps" stacks/node/scripts/ralph-loop.sh && grep -q "result.json" stacks/node/scripts/ralph-loop.sh && grep -q "kickoff-prompt" stacks/node/scripts/ralph-loop.sh && grep -q "gh pr create" stacks/node/scripts/ralph-loop.sh && echo "PASS"</automated>
  </verify>
  <done>ralph-loop.sh restarts Claude Code up to MAX_RESTARTS times when checklist has incomplete tasks. Writes status.json on each iteration. Captures JS after-snapshots (package.json deps + lockfile hash). Builds result.json. Pushes branch and creates PR when GIT_PUSH=true and GH_TOKEN set. PR title varies by STACK_TYPE.</done>
</task>

<task type="auto">
  <name>Task 2: Create stream-pretty.sh and kickoff-prompt.txt</name>
  <files>stacks/node/scripts/stream-pretty.sh, stacks/node/kickoff-prompt.txt</files>
  <action>
**stream-pretty.sh** (`stacks/node/scripts/stream-pretty.sh`):
Copy `stacks/laravel/scripts/stream-pretty.sh` verbatim. This script is stack-agnostic — it processes Claude Code's stream-json output format which is the same regardless of what the agent is doing. Read the Laravel file and create an identical copy at the node path.

**kickoff-prompt.txt** (`stacks/node/kickoff-prompt.txt`):
Create the initial prompt sent to Claude Code on each launch/restart. This is what Claude Code sees first.

Content:
```
Read .upgrade/CLAUDE.md for your instructions, then read .upgrade/plan.md for the upgrade plan, then read .upgrade/checklist.yaml for current progress.

Pick the next task with status "not_started" and begin working on it. After completing each task:
1. Run .upgrade/scripts/verify-fast.sh to check your work
2. Update the task status in .upgrade/checklist.yaml
3. Append a brief entry to .upgrade/run-log.md with what you did and the verify result
4. Update .upgrade/changelog.md with user-facing changes
5. Commit your changes (one commit per phase)

If you get stuck on a task after 3 attempts, mark it as "failed" with a reason in checklist.yaml, log the failure in run-log.md, and move to the next task.

If all tasks are complete or failed, run .upgrade/scripts/verify-full.sh as a final check and stop.
```

This is a generic kickoff that works for all three JS stacks because the stack-specific behavior is in the CLAUDE.md and plan.md templates (populated by Phase 2).
  </action>
  <verify>
    <automated>cd /Users/mariomeyer/code/ReyemTech/stack-upgrade && test -f stacks/node/scripts/stream-pretty.sh && test -f stacks/node/kickoff-prompt.txt && grep -q "checklist.yaml" stacks/node/kickoff-prompt.txt && grep -q "verify-fast" stacks/node/kickoff-prompt.txt && echo "PASS"</automated>
  </verify>
  <done>stream-pretty.sh is an exact copy of the Laravel version (stack-agnostic). kickoff-prompt.txt instructs Claude Code to read .upgrade/ files, pick next task, verify, update status, commit, and handle stuck tasks. Works for all three JS stacks.</done>
</task>

</tasks>

<verification>
1. `stacks/node/scripts/ralph-loop.sh` has write_status, restart logic, after-snapshots, result.json, push+PR
2. `stacks/node/scripts/stream-pretty.sh` exists and matches Laravel version
3. `stacks/node/kickoff-prompt.txt` instructs agent to read .upgrade/ files and follow checklist
4. Ralph loop uses JS-specific snapshot commands (jq on package.json, not composer show)
5. PR title is dynamic based on STACK_TYPE
</verification>

<success_criteria>
- ralph-loop.sh is a faithful port of Laravel's with only JS-specific snapshot and branch changes
- stream-pretty.sh is byte-for-byte identical to Laravel version
- kickoff-prompt.txt references all three .upgrade/ files and both verify scripts
- After-snapshots write after-package-deps.json and after-lockfile-hash.txt to /output/
- result.json contains all required fields (outcome, exit_code, total_phases, completed, failed, restarts, elapsed_minutes, branch)
</success_criteria>

<output>
After completion, create `.planning/phases/01-shared-node-image-foundation/01-03-SUMMARY.md`
</output>
